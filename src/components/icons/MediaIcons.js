import React from 'react';
import { Play, Pause } from "@solar-icons/react";

export const PlayIcon = (props) => <Play size={18} {...props} />;
export const PauseIcon = (props) => <Pause size={18} {...props} />;